import { memo } from 'react'
import "./index.scss"

const BackgroundCanvas = memo(() => {
    return (
        <div className="banner-canvas-container">
          1235
        </div>
    )
})

export default BackgroundCanvas